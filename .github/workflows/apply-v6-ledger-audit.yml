name: Apply V6 ledger audit integration
on:
  push:
    branches: [improve/v6-max-predictive]
    paths:
      - tools/apply_v6_ledger_audit_patch.py
      - .github/workflows/apply-v6-ledger-audit.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: improve/v6-max-predictive
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python tools/apply_v6_ledger_audit_patch.py
      - run: python -m py_compile app_mlb.py modules/pick_ledger.py
      - name: Commit V6 ledger audit integration
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app_mlb.py
          git commit -m 'Apply V6 ledger audit integration'
          git push origin HEAD:improve/v6-max-predictive
